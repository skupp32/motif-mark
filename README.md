# motif-mark

This repository has a script to find all locations of motif binding sites and plots them per fasta file.

Input:

Path to fasta file (-f in argparse)
Path to file with motifs (-m in argparse)

Output:

PNG per fasta file with each gene drawn with its exons and motifs



